using Microsoft.AspNetCore.Mvc;
using nov30task.ViewModels.SlidersVM;
using nov30task.ViewModels.TagsVM;

namespace nov30task.Areas.Admin.Controllers
{
    [Area("Admin")]
    public class TagsController : Controller
    {

        /*public IActionResult Index()
        {
            return View();
        }

        public async Task<IActionResult> CreateTag(TagCreateVM vm)
        {
            var datas = await _db.Tags.Select(s => new SliderListItemVM
            {
                Name = s.Name
            }).ToListAsync();
            return View(datas);
        }

        public async Task<IActionResult> Create(TagCreateVM vm)
        {
            var data =
            return View(data);
        }*/

    }
}
